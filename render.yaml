services:
  - type: web
    name: nekomasu-ai-proxy
    runtime: docker # 修改為 Docker 環境
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 4000
